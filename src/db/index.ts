/**
 * src/db/index.ts
 *
 * Database client placeholder.
 *
 * Once Drizzle ORM and the appropriate driver (better-sqlite3 for local,
 * @libsql/client for Turso, or `pg` for PostgreSQL) are installed, this
 * module will:
 *
 *  1. Read DATABASE_PROVIDER from the validated env config.
 *  2. Instantiate the correct Drizzle client via the provider factory.
 *  3. Export a singleton `db` instance used throughout the application.
 *
 * Usage (future):
 *   import { db } from "@/db";
 *   const accounts = await db.select().from(schema.accounts);
 */

// No implementation yet — Drizzle ORM installed in a subsequent task.
export {};
